name: novident_spell_checker
description: "Spelling correction & fuzzy search based on the symmetric delete spelling correction algorithm. Dart port of symspell-ex with some extra things."
version: 1.0.0
homepage:

environment:
  sdk: ">=3.4.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
