# Lint commit messages with commitlint (Conventional Commits).
#
# Runs on every push to master and every pull request targeting master.
# The convention is defined in commitlint.config.mjs at the repo root.
#
# To block PRs with non-conforming commits, enable branch protection on master:
#   Settings → Branches → Branch protection rules → "Require status checks
#   to pass before merging" → select "Commitlint".

name: Commit Message Lint

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: commitlint-${{ github.ref }}
  cancel-in-progress: true

jobs:
  commitlint:
    name: Commitlint
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@eef61447b9ff4aafe5dcd4e0bbf5d482be7e7871 # v4.2.1
        with:
          # Full history so push events can resolve the full commit range.
          fetch-depth: 0

      - name: Lint commit messages
        uses: wagoid/commitlint-github-action@b948419dd99f3fd78a6548d48f94e3df7f6bf3ed # v6.2.1
